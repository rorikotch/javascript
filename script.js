  /*var cheese = document.querySelector("#" + id);

  // Now that we have the province the user has interacted with, let's
  // store that node in a variable to work with further.

  if (cheese.hasAttribute("class") == false) {
    cheese.setAttribute("class", "selected");
  } else {
    cheese.removeAttribute("class");
  }

  // The above if statement creates a simple toggle effect. If there is
  // no class on the element clicked, apply the class selected (we've
  // created the style in .css). If it has the class attribute (and is
  // therefore selected) and is clicked again, remove the attribute.

});
*/

var cheese;

if (cheese.visible("class") == false) {
  console.log("working");
} else {
  cheese.visibl("class")== true;
}
